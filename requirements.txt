# Cortex Lambda dependencies -- zip deployment, no torch, no ECR.
# Verified against current PyPI as of Aug 2026. If `sam build` fails on any of
# these, re-run `pip index versions <package>` and bump -- don't just delete pins.
#
# boto3/botocore are deliberately NOT listed here: every Lambda Python runtime
# ships them pre-installed, bundling your own copy only bloats the package.

fastapi==0.115.6
mangum==0.19.0
fastembed==0.8.0
asyncpg==0.30.0
groq==1.6.0
mcp==1.9.4
langgraph==1.2.11
httpx